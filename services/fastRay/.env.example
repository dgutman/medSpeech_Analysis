# Ray Serve Configuration
# Copy this file to .env and adjust values as needed

# Number of Ray Serve replicas (workers)
# Recommended: 40 replicas (10 per GPU) for 4 L40S GPUs
NUM_REPLICAS=40

# Number of GPUs per replica (fractional GPUs supported)
# For 40 replicas across 4 GPUs: 4 / 40 = 0.1 GPU per replica
NUM_GPUS_PER_REPLICA=0.1

# Whisper model to use (optional, defaults to large-v3)
# Options: tiny, base, small, medium, large, large-v2, large-v3
# WHISPER_MODEL=large-v3

# Compute type (optional, defaults to float16)
# Options: float16, float32, int8_float16
# COMPUTE_TYPE=float16

# Model cache size (optional, defaults to 2)
# Maximum number of models to keep in memory per replica
# MODEL_CACHE_SIZE=2
